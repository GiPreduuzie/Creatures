<Window x:Class="ImpossibleCreatures.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImpossibleCreatures"
        mc:Ignorable="d"
        Name="Window"
        Title="MainWindow" Height="1000" Width="1000" Loaded="Start" >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Column="0" ColumnSpan="2" Row="0" Name="MenuRow" Width="auto" Height="auto" Background="White">
            <DockPanel>
                <Menu DockPanel.Dock="Top">
                    <MenuItem Header="Show energy" IsCheckable="True" Name="MenuItemEnergy" />
                </Menu>
                <TextBox AcceptsReturn="True" />
            </DockPanel>
        </Grid>
        <Grid Column="0" Row="1">
            <Button Width="auto" Height="auto" Name="start" Click="start_Click" Foreground="Black">Start/Pause</Button>
        </Grid>
        <Grid Column="1" Row="1">
            <Button Name="onestep" Click="onestep_Click">One step forward</Button>
        </Grid>
        <Grid Column="0" ColumnSpan="2" Row="2" Name="MainGrid" Width="auto" Height="auto" Background="White"/>
    </Grid>

</Window>
